import 'package:flutter/material.dart';

class TaskLabel {
  final int id;
  final String name;
  final Color color;
  TaskLabel({
    required this.id,
    required this.name,
    required this.color,
  });
}